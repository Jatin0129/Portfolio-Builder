import { geopoliticalEvents } from "@/mock-data";

export function getGeopoliticalEvents() {
  // Future integration point: map real-time geopolitical signals into severity-scored event objects.
  return geopoliticalEvents;
}
